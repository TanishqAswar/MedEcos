# Git Workflow Rules

- **Always Commit & Push Changes**: After completing code modifications or task steps, always stage changes (`git add .` or specific files), commit with a descriptive message (`git commit -m "..."`), and push (`git push`) to the remote repository.
